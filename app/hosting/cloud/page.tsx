import type { Metadata } from "next";
import { StandardProductPage } from "@/components/standard-product-page";

export const metadata: Metadata = { title: "Managed Cloud Hosting", description: "Managed cloud hosting for higher-traffic and resource-intensive HostMyWeb workloads." };

export default function CloudPage() {
  return <StandardProductPage eyebrow="Hosting / Managed Cloud" title="Dedicated resources without" accent="going it alone." description="Managed Cloud is the next step when a project needs dedicated capacity but the customer still wants a managed hosting environment rather than full server administration." primaryHref="/#custom" primaryLabel="Request Managed Cloud" secondaryHref="/hosting" secondaryLabel="Compare Hosting Types" sectionTitle="For workloads that have outgrown shared hosting." sectionText="Managed Cloud is kept separate from the shared plan grid because it solves a different problem: dedicated resources, larger workloads, and application-specific server configuration." features={[{label:"RESOURCES",title:"Dedicated cloud capacity",text:"A better fit for workloads that should not share the same resource model as ordinary business websites."},{label:"MANAGED",title:"Managed infrastructure",text:"Keep server and hosting management assistance around the environment instead of taking on an entirely self-managed VPS."},{label:"ECOMMERCE",title:"Higher-traffic commerce",text:"Suitable for larger stores and transaction-heavy sites that need a stronger resource baseline."},{label:"APPLICATIONS",title:"Application-focused profiles",text:"Cloud configurations can be selected around the application stack and workload requirements."},{label:"DATABASES",title:"Larger data workloads",text:"Use a dedicated-resource product when database requirements no longer fit ordinary shared hosting."},{label:"SCALE",title:"Provider options by configuration",text:"Managed cloud can be configured through supported cloud providers and regions based on the service being ordered."}]} links={[{title:"Shared Cloud Hosting",text:"Start here when dedicated resources are not actually required.",href:"/hosting/shared"},{title:"VPS Hosting",text:"Choose a VPS when operating-system and software control matter more than a managed environment.",href:"/hosting/vps"}]} notice="Managed Cloud is configured to order until HostMyWeb publishes specific automated cloud products, regions, and pricing. The page does not pretend instant checkout exists before those provider configurations are live." />;
}
